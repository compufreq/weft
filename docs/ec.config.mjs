// Expressive Code config — auto-loaded by astro-expressive-code/Starlight.
// Wrap long code lines instead of horizontal scrolling: scrollable regions
// need keyboard focus (axe: scrollable-region-focusable); wrapping sidesteps
// that statically, with no JS required.
export default {
  defaultProps: { wrap: true },
};
